import { RouteLoadingShell } from "@/components/navigation/RouteLoadingShell";

export default function AppLoading() {
  return (
    <RouteLoadingShell
      title="Page"
      description="Loading route shell and content."
    />
  );
}
